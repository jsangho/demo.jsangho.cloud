---
layout: report
title: 사이트 구축
permalink: /03-site-setup/
chapter: 3
---

## 1) Jekyll 사이트 생성

*작성 예정 — `jekyll new . --force` 실행 결과와 기존 파일 보존 여부를 기록한다.*

## 2) 디렉터리 구조

*작성 예정 — 아래 트리에 각 항목의 역할 설명을 붙인다.*

```
├── _layouts/       cover(표지), report(본문)
├── assets/         main.scss — minima 테마 오버라이드
├── _config.yml     사이트 설정
├── index.md        표지
└── toc.md          목차
```

## 3) 로컬 미리보기 서버 실행

*작성 예정 — `--host 0.0.0.0` 이 필요한 이유(외부 기기 접근)를 함께 기록한다.*
