---
layout: report
title: AI 에이전트 설계
permalink: /04-agent/
chapter: 4
---

본 장은 [제 5 장]({{ '/05-recruitment/' | relative_url }})의 매칭과 [제 6 장]({{ '/06-verification/' | relative_url }})의 검증을 실제로 수행하는 에이전트 계층을 다룬다. 두 장에서 "에이전트가 처리한다"고 기술한 지점의 구현 설계가 여기에 해당한다.

## 1) 에이전트 구성

### 역할 분리

하나의 에이전트에 모든 역할을 맡기지 않는다. 매칭과 검증은 요구되는 판단의 성격이 다르고, 실패했을 때의 영향 범위도 다르다. 매칭이 틀리면 경기 한 건의 품질이 떨어지지만, 검증이 틀리면 사용자의 스탯이라는 지속되는 기록이 오염된다.

| 에이전트 | 역할 | 호출 시점 |
|---|---|---|
| 매칭 | 공고 조건과 후보 스탯을 대조해 순위와 근거를 만든다 | 후보 열람, 공고 마감 |
| 검증 | 상대 팀 평가에서 항목별 신호를 추출해 집계한다 | 경기 종료 후 평가 수집 완료 |
| 요약 | 경기 기록을 스탯 변동 입력으로 환산한다 | 검증 집계 이후 |

### 호출 흐름

에이전트는 상시 실행되지 않고 위 시점에만 호출된다. 이용자의 요청을 그대로 모델에 흘려보내는 구조가 아니라, 서버가 필요한 데이터를 모아 호출하고 결과를 검증한 뒤 저장하는 구조다. 모델 응답은 곧바로 이용자에게 노출되지 않으며, 서버가 형식과 값의 범위를 확인한 뒤 화면에 반영한다.

## 2) 매칭 에이전트

### 입력과 출력

| 구분 | 내용 |
|---|---|
| 입력 | 공고 조건(종목·일시·장소·포지션·요구 실력대), 지원자 목록과 각자의 스탯·신뢰도·노쇼 이력 |
| 출력 | 순위가 매겨진 추천 목록, 각 추천의 근거 문장, 조건 완화 여부와 완화 항목 |

### 출력 형식을 스키마로 고정한다

출력은 자유 서술이 아니라 스키마로 고정된 구조화 출력으로 받는다. 순위와 후보 식별자가 자유 문장에 섞여 나오면 서버가 파싱에 실패할 여지가 생기고, 실패를 재시도로 덮으면 비용과 지연이 함께 늘어난다. 근거 문장만 자연어 필드로 두고, 나머지 값은 형식이 보장되는 필드로 받는다.

### 순위 산출

요소별 가중치 자체는 모델이 정하지 않는다. 포지션 적합도·실력대 근접도·거리·신뢰도·노쇼 이력의 수치화는 서버가 계산해 입력으로 넘기고, 모델은 그 수치만으로 판단하기 어려운 부분 — 부포지션 수행 가능성, 평가 서술에 드러난 특성, 조건 완화의 타당성 — 을 반영해 순서를 정하고 근거를 쓴다. 가중치를 모델에 맡기면 같은 조건에서 순위가 흔들리고, 왜 그렇게 나왔는지 설명할 수 없게 된다.

### 근거 문장의 용도

근거 문장은 장식이 아니라 운영자의 판단 재료다. [제 5 장]({{ '/05-recruitment/' | relative_url }})에서 최종 확정을 사람이 하도록 설계한 이상, 운영자가 추천을 검토할 수 있어야 한다. 근거에는 어떤 조건이 맞았고 무엇이 완화됐는지가 드러나야 한다.

## 3) 도구 및 프롬프트 설계

### 도구 구성

에이전트에 노출하는 도구는 최소로 둔다. 도구가 많아질수록 모델이 잘못된 도구를 고를 여지가 커지고, 정의 자체가 매 호출의 비용에 실린다.

| 도구 | 용도 | 부수 효과 |
|---|---|---|
| 후보 조회 | 지원자의 스탯·신뢰도·이력을 읽는다 | 없음 |
| 평가 조회 | 특정 경기의 평가 자료를 읽는다 | 없음 |
| 기록 | 집계 결과를 저장한다 | 있음 — 서버 검증 후 반영 |

부수 효과가 있는 도구는 모델이 직접 확정하지 않는다. 기록 도구의 호출 결과는 서버가 값의 범위와 정합성을 확인한 뒤에야 반영된다.

### 도구 설명에 호출 시점을 적는다

도구 설명에는 그 도구가 무엇을 하는지뿐 아니라 **언제 호출해야 하는지**를 함께 적는다. 기능만 적힌 설명은 모델이 도구를 덜 쓰게 만든다. 반대로 "반드시", "무조건" 같은 강조를 넣으면 불필요한 호출이 늘어난다. 조건을 서술하되 강조하지 않는 것이 기준이다.

### 프롬프트 구성

프롬프트는 앞쪽에 고정 내용(역할, 판단 기준, 출력 규칙)을 두고 뒤쪽에 매 호출 달라지는 내용(공고, 후보 목록)을 둔다. 이 순서를 지키면 고정 부분이 캐시되어 반복 호출의 비용과 지연이 줄어든다. 시각이나 요청 식별자처럼 매번 바뀌는 값을 앞쪽에 넣으면 캐시가 무효화되므로, 그런 값은 반드시 뒤쪽에 배치한다.

## 4) 모델 선정

### 후보

Anthropic Claude 계열을 후보로 둔다. 2026년 6월 기준 공개 단가는 아래와 같다(Anthropic 자사 API 기준, 100만 토큰당).

| 모델 | 모델 ID | 컨텍스트 | 입력 | 출력 |
|---|---|---|---|---|
| Claude Opus 5 | `claude-opus-5` | 1M | $5.00 | $25.00 |
| Claude Sonnet 5 | `claude-sonnet-5` | 1M | $3.00 | $15.00 |
| Claude Haiku 4.5 | `claude-haiku-4-5` | 200K | $1.00 | $5.00 |

Claude Sonnet 5는 2026년 8월 31일까지 도입 단가 $2.00 / $10.00이 적용된다. 단가는 변동될 수 있으므로 산정 시점을 함께 기록한다.

### 선정 기준

| 기준 | 내용 |
|---|---|
| 판단 품질 | 조건이 상충하는 상황에서 납득 가능한 순위를 내는가 |
| 응답 지연 | 운영자가 후보 목록을 열었을 때 기다릴 수 있는 시간 안에 오는가 |
| 호출 비용 | 경기 1건당 누적 비용이 감당 가능한가 |

세 기준은 서로 맞선다. [제 2 장]({{ '/02-requirements/' | relative_url }})의 NFR-01(응답 시간)과 NFR-07(모델 비용)이 이 절충을 요구사항으로 명시한 항목이다.

### 역할별 배치

모든 에이전트에 같은 모델을 쓸 이유는 없다. 검증 에이전트의 신호 추출은 정형화된 작업이라 낮은 단가의 모델로 충분할 수 있고, 매칭 에이전트는 조건이 상충할 때의 판단이 결과를 좌우하므로 상위 모델이 유리하다. 다만 역할별로 모델을 나누면 검증 대상이 늘어나므로, 단일 모델로 시작해 시험 결과를 근거로 분리를 판단한다. 〔확인 필요: 역할별 모델 배치 확정〕

### 비용 산정 방식

단가만으로는 실제 비용을 알 수 없다. 경기 1건당 비용은 아래 요소로 결정된다.

- 매칭 호출 횟수 — 후보 열람과 마감 두 시점 ([제 5 장]({{ '/05-recruitment/' | relative_url }}))
- 호출당 입력 토큰 — 고정 프롬프트 + 후보 수에 비례하는 부분
- 캐시 적중 여부 — 고정 프리픽스가 재사용되면 해당 구간은 크게 낮은 단가로 계산된다

따라서 실측 없이 산정한 값은 근거가 되지 못한다. 대표 공고를 표본으로 토큰 수를 실측해 산정하고, 결과를 [제 7 장]({{ '/07-testing/' | relative_url }})에 기록한다. 〔확인 필요: 최종 선정 모델과 경기 1건당 실측 비용〕
