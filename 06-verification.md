---
layout: report
title: 실력 검증 기능 개발
permalink: /06-verification/
chapter: 6
---

본 장은 [제 5 장]({{ '/05-recruitment/' | relative_url }})에서 확정된 참여자의 실력을 어떻게 수치로 두고, 그 수치를 어떻게 믿을 만한 값으로 만드는지 다룬다.

문제의 출발점은 실력 정보의 출처가 본인이라는 데 있다. 자기 신고만으로 만든 스탯은 과장되기 쉽고, 과장된 스탯이 매칭에 그대로 쓰이면 경기 수준이 무너져 구인 기능 자체의 신뢰가 함께 무너진다. 따라서 본 사업은 스탯을 만드는 일과 그 스탯을 검증하는 일을 분리해 설계했다.

## 1) 스탯 체계 설계

### 스탯 항목

스탯은 종목 공통 항목과 포지션 항목으로 나눈다. 공통 항목은 어떤 포지션이든 비교 가능하게 두고, 포지션 항목은 해당 포지션을 지원할 때만 매칭에 반영한다.

| 구분 | 항목 | 매칭 반영 |
|---|---|---|
| 공통 | 체력, 스피드, 경기 이해도, 매너 | 항상 |
| 포지션 | 〔확인 필요: 종목별 포지션 항목 확정〕 | 해당 포지션 지원 시 |

항목 수를 늘리면 정밀해지지만 평가자가 채워야 할 칸이 늘어 응답률이 떨어진다. 공통 항목을 넷으로 제한한 것은 이 때문이다.

### 표기 방식

스탯은 내부적으로 수치로 관리하고, 화면에는 등급 구간으로 표시한다. 수치를 그대로 노출하면 1~2점 차이를 실력 차로 받아들여 분쟁이 생기는데, 그 정도 차이는 표본이 적을 때 의미가 없다. 〔확인 필요: 등급 구간 수와 경계값〕

### 초기값과 갱신

신규 이용자의 스탯은 자기 신고값으로 시작하되 **미검증 상태**로 둔다. 미검증 스탯은 화면에서 잠정값으로 구분해 표시하고, 매칭 순위에서도 검증된 스탯보다 뒤로 밀린다.

갱신은 경기가 `종료` 상태로 넘어간 뒤 아래 규칙을 따른다.

- 한 경기로 움직일 수 있는 변동 폭에 상한을 둔다. 한 번의 부진이나 활약이 스탯을 뒤집으면 값이 요동쳐 매칭에 쓸 수 없다.
- 최근 경기에 더 큰 가중을 준다. 〔확인 필요: 가중 감쇠 기간〕
- 표본이 임계치에 못 미치는 동안에는 미검증 상태를 유지한다.

## 2) 레드팀 교차 검증

### 검증 주체를 상대 팀으로 둔 이유

같은 팀 동료의 평가는 검증 자료로 쓰지 않는다. 함께 부르고 함께 뛰는 관계에서는 평가가 후해지고, 반복되면 서로의 스탯을 올려 주는 담합이 성립한다. 그날 맞붙은 **상대 팀**은 이해관계가 반대이므로 과장을 걸러 낼 유인이 있다. 본 사업이 검증 절차를 레드팀이라 부르는 것은 이 구도에서 왔다.

### 검증 절차

1. 경기가 `종료` 상태로 넘어가면 양 팀 참여자에게 상대 팀 참여자에 대한 평가를 요청한다.
2. 평가는 항목별 선택과 자유 서술을 함께 받는다. 자유 서술을 받는 이유는 숫자만으로는 왜 그렇게 평가했는지가 남지 않기 때문이다.
3. 검증 에이전트가 자유 서술에서 항목별 신호를 추출해 선택값과 함께 집계한다. 에이전트 구성은 [제 4 장]({{ '/04-agent/' | relative_url }})에 기술한다.
4. 집계값과 본인 신고값의 괴리를 계산한다.
5. 괴리가 임계를 넘으면 스탯을 집계값 쪽으로 보정하고 신뢰도를 낮춘다. 〔확인 필요: 괴리 임계와 보정 폭〕

### 평가 왜곡에 대한 대응

상대 팀 평가에도 고유한 왜곡이 있다. 설계에 반영한 대응은 다음과 같다.

| 왜곡 | 내용 | 대응 |
|---|---|---|
| 승패 편향 | 크게 진 팀이 상대를 과대평가하거나 반대로 박하게 평가 | 경기 결과를 함께 기록해 편향 보정에 사용 |
| 표본 부족 | 평가자가 한두 명이면 개인 취향이 그대로 결과가 됨 | 최소 평가 수 미달 시 검증 미완료로 처리 |
| 반복 담합 | 같은 인원끼리만 계속 맞붙으며 평가를 주고받음 | 평가자 다양성이 낮으면 가중을 낮춤 |
| 보복 평가 | 낮은 평가를 받은 쪽이 되갚음 | 평가는 상호 비공개, 제출 완료 후 집계값만 공개 |

### 평가 미제출

평가는 강제하지 않는다. 다만 평가를 제출하지 않은 이용자는 자신의 검증도 진행되지 않으므로, 미검증 상태가 유지되어 매칭에서 불리해진다. 제재가 아니라 구조로 참여를 유도하는 방식이다.

## 3) 신뢰도 지표 산출

### 산출 요소

신뢰도는 스탯이 얼마나 검증을 거쳤는지를 나타내는 별도 값이다. 실력이 높다는 뜻이 아니라 **표시된 실력을 믿을 만하다**는 뜻이며, 둘을 섞지 않도록 화면에서도 분리해 표시한다.

| 요소 | 방향 | 근거 |
|---|---|---|
| 검증 경기 수 | 많을수록 높음 | 표본 크기 |
| 평가자 다양성 | 넓을수록 높음 | 담합·편중 억제 |
| 평가 일관성 | 일관될수록 높음 | 평가가 갈리면 값이 불안정하다 |
| 최근성 | 최근일수록 높음 | 오래된 검증은 현재 실력을 보장하지 않는다 |

〔확인 필요: 요소별 가중치와 산출식〕 산출식은 시험을 거쳐 확정하고 결과를 [제 7 장]({{ '/07-testing/' | relative_url }})에 기록한다.

### 표시 등급

| 등급 | 조건 | 화면 표기 |
|---|---|---|
| 미검증 | 검증 경기 수 0 | 잠정값으로 표시 |
| 검증중 | 최소 표본 미달 | 표본 수 함께 표시 |
| 검증됨 | 최소 표본 충족 | 스탯을 확정값으로 표시 |

〔확인 필요: 최소 표본 기준〕

### 낮은 신뢰도를 배제하지 않는 이유

신뢰도가 낮은 이용자를 매칭에서 제외하면 검증 기회 자체가 없어져 신규 유입이 영구히 미검증에 머문다. 따라서 신뢰도는 순위 요소로만 반영하고, 배제 기준으로는 쓰지 않는다. 운영자에게는 후보의 신뢰도 등급을 함께 보여 판단에 맡긴다.
